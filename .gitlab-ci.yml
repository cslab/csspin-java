# -*- mode: yaml; coding: utf-8 -*-

include:
    - project: qs/spin/spin_plugin_common
      ref: v1.7.0
      file: ci/.gitlab-ci.yml

# ------------------------------------------------------------------------------
# FIXME: Remove the following customizations as soon as the documentation for
#        this plugin-package is added.
.release_deps:
    needs:
        - wheel
        - job: test_linux
          artifacts: false
        - job: test_windows
          artifacts: false

documentation:
    rules:
        - when: never

pages:
    rules:
        - when: never
